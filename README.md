# Schooll-project-repo

Schooll-project-repo is a Python Django project that handles the storage of images of vehicles that overspeed on a certain road where the rasberry Pi is deployed.

## Installation

Use the package manager [pip](https://pip.pypa.io/en/stable/) to install the neccessary packages.

```bash
pip install -r reqirements.txt
```
